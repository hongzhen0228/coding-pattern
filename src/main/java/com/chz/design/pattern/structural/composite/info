组合模式

定义：将对象组合成树型结构，表示 部分，整体的结构

使客户单对单个对象和组合对象保持一致的方式处理

结构型

希望客户端可以忽略组合对象个单个对象的差异时

处理一个树形结构时

清楚的定义分层次的复杂对象，表示对象的全部或者部分层次

优点：

让客户端忽略了层次的差异，方便对整个层次结构进行控制

简化客户端代码

符合开闭原则

缺点：

限制类型时候，较为复杂

使设计变得更加抽象